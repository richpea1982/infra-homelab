---
- name: Configure Jellyfin LXC (GPU-pinned, pve2)
  hosts: media_lxc
  become: true
  gather_facts: true

  roles:
    - role: apt_mirror
    - role: jellyfin
      when: inventory_hostname == 'jellyfin'
