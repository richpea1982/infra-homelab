---
- name: Add user comforts to k3s nodes
  hosts: k3s_cluster
  become: true
  gather_facts: true
  vars_files:
    - ../group_vars/all/vault.yml

  tasks:
    - name: Add user comforts to k3s nodes
      ansible.builtin.import_role:
        name: k3s_cluster
        tasks_from: user-comforts
