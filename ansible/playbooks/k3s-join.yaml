---
- name: Orchestrate K3s Node Join
  hosts: k3s_cluster
  become: true
  gather_facts: true
  vars_files:
    - ../group_vars/all/vault.yml

roles:
  - role: ../roles/apt_mirror
  - role: ../roles/k3s_cluster
    tasks_from: rejoin
