---
- name: Ensure legacy iptables alternative mappings are active
  community.general.alternatives:
    name: "{{ item }}"
    path: "/usr/sbin/{{ item }}-legacy"
  loop:
    - iptables
    - ip6tables
    - arptables
    - ebtables

- name: Ensure /etc/rancher/k3s directory exists
  file:
    path: /etc/rancher/k3s
    state: directory
    mode: '0755'
    owner: root
    group: root

- name: Ensure standard upstream nameserver fallback is declared
  copy:
    dest: /etc/rancher/k3s/resolv.conf
    content: |
      nameserver 1.1.1.1
      nameserver 8.8.8.8
    mode: '0644'
    owner: root
    group: root

- name: Turn off swap space completely for current session
  command: swapoff -a
  changed_when: false

- name: Disable swap in fstab persistently across reboots
  replace:
    path: /etc/fstab
    regexp: '^([^#].*\sswap\s.*)$'
    replace: '#\1'
