---
# Delete the default StorageClass (reclaimPolicy is immutable, so we must delete and recreate)
- name: Delete default local-path StorageClass
  kubernetes.core.k8s:
    state: absent
    kind: StorageClass
    name: local-path
  ignore_errors: true

# Apply the retained version cleanly using inline definition
- name: Apply retained StorageClass
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: storage.k8s.io/v1
      kind: StorageClass
      metadata:
        name: local-path
        annotations:
          storageclass.kubernetes.io/is-default-class: "true"
      provisioner: rancher.io/local-path
      volumeBindingMode: WaitForFirstConsumer
      reclaimPolicy: Retain
